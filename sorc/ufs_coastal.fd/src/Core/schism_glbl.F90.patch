--- schism_glbl.F90.orig
+++ schism_glbl.F90
@@ Add after line 154 (after "real(rkind),save :: time_stamp") @@

   real(rkind),save :: time_stamp !simulation time in sec
+  real(rkind),save :: time_hotstart = 0.d0 !hotstart time in sec (for ihot=1 tidal phase correction)

 Context: This variable stores the actual simulation time from hotstart.nc
 BEFORE the ihot=1 reset. It is used in schism_init.F90 to adjust the
 tidal equilibrium argument (face) so that tidal boundary conditions
 have the correct phase when time is reset to 0.
