"""Utility helpers for nos_workflow.

Lightweight modules that translate, validate, or shuttle data between
NCO env, YAML configuration, and shell scripts. Anything heavier (forcing
math, subprocess wrappers, NCO bridge logic) belongs in
``nos_workflow.bash_compat`` or ``nos_utils``.
"""
